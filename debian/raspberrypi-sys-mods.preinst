#! /bin/sh

set -e

if [ upgrade != "$1" ] || dpkg --compare-versions "$2" lt 1:20250627; then
    dpkg-divert --package raspberrypi-sys-mods --add --rename \
        --divert /usr/lib/python3.13/EXTERNALLY-MANAGED.orig /usr/lib/python3.13/EXTERNALLY-MANAGED
fi

#DEBHELPER#

exit 0
